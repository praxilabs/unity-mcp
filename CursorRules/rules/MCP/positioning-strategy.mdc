---
description: only used when the user asks to create a new step node graph, and it should be always applied at this situation as it is a node positioning techniques.
alwaysApply: false
---
## 🎯 **POSITIONING DECISION MATRIX** ⚠️ **MANDATORY**

| Graph Type | Node Count | **Required Strategy** | **Rationale** |
|---|---|---|---|
| Simple Linear | 3-7 nodes | **Left-Right (DEFAULT)** | Maintains simplicity |
| Equipment Setup | 4-7 nodes | **Left-Right (DEFAULT)** | Sequential flow is clear |
| Interactive Lab | 8+ nodes | **Top-Down Staging (MANDATORY)** | Prevents horizontal scrolling |
| Complex Experiment | 8+ nodes | **Top-Down Staging (MANDATORY)** | Improves visual organization |
| Multi-Route | Any count | **Top-Down Staging (MANDATORY)** | Better parallel visualization |

---

## 🚫 **FORBIDDEN POSITIONING PATTERNS** ⚠️

### **❌ NEVER DO:**
- **NEVER stack nodes on top of each other** (same X position)
- **NEVER force vertical flow for simple experiments** (3-7 nodes)
- **NEVER create unorganized node clusters** without logical grouping
- **NEVER ignore user experience** - prioritize readability over rigid positioning

### **✅ ALWAYS DO:**
- **ALWAYS start with left-right flow** for simple graphs
- **ALWAYS switch to staging when graph complexity increases**
- **ALWAYS group related nodes logically** within stages
- **ALWAYS consider user navigation experience**

---

## 🛠️ **IMPLEMENTATION EXAMPLES**

### **Simple Graph (Left-Right)**
```bash
# 5 nodes - Left-right flow
mcp_unityMCP_create_xnode_node --position_x 0 --position_y 0      # Start
mcp_unityMCP_create_xnode_node --position_x 450 --position_y 0    # Step 1
mcp_unityMCP_create_xnode_node --position_x 900 --position_y 0    # Step 2
mcp_unityMCP_create_xnode_node --position_x 1350 --position_y 0   # Step 3
mcp_unityMCP_create_xnode_node --position_x 1800 --position_y 0   # Complete
```

### **Complex Graph (Top-Down Staging)**
```bash
# 12 nodes - Top-down staging
# Stage 1: Setup
mcp_unityMCP_create_xnode_node --position_x 0 --position_y 0      # Start
mcp_unityMCP_create_xnode_node --position_x 450 --position_y 0    # Setup
mcp_unityMCP_create_xnode_node --position_x 900 --position_y 0    # Initialize

# Stage 2: Main Operations
mcp_unityMCP_create_xnode_node --position_x 0 --position_y 300    # Operation A
mcp_unityMCP_create_xnode_node --position_x 450 --position_y 300  # Operation B
mcp_unityMCP_create_xnode_node --position_x 900 --position_y 300  # Operation C

# Stage 3: Validation
mcp_unityMCP_create_xnode_node --position_x 0 --position_y 600    # Check A
mcp_unityMCP_create_xnode_node --position_x 450 --position_y 600  # Check B
mcp_unityMCP_create_xnode_node --position_x 900 --position_y 600  # Check C

# Stage 4: Completion
mcp_unityMCP_create_xnode_node --position_x 0 --position_y 900    # Finalize
mcp_unityMCP_create_xnode_node --position_x 450 --position_y 900  # Cleanup
mcp_unityMCP_create_xnode_node --position_x 900 --position_y 900  # End
```

---

## 🔄 **ADAPTIVE POSITIONING WORKFLOW**

### **Step 1: Assess Graph Complexity**
- Count total nodes
- Identify parallel operations
- Determine logical stages

### **Step 2: Choose Strategy**
- **3-7 nodes**: Left-right flow
- **8+ nodes**: Top-down staging
- **Complex logic**: Always staging

### **Step 3: Implement Positioning**
- Apply chosen strategy consistently
- Maintain logical grouping
- Ensure visual clarity

### **Step 4: Validate User Experience**
- Check horizontal scrolling requirements
- Verify logical flow clarity
- Ensure professional appearance

---

## 🎯 **KEY PRINCIPLES**

1. **User Experience First**: Prioritize readability and navigation
2. **Adaptive Strategy**: Switch from left-right to staging when needed
3. **Logical Grouping**: Organize nodes by function and stage
4. **Visual Hierarchy**: Use positioning to show relationships
5. **Professional Appearance**: Maintain clean, organized layouts

**Remember**: The goal is optimal user experience, not rigid adherence to a single positioning method.


**The graph structure determines positioning strategy, NOT personal preference. Always analyze first, position second.**
